DSEG	SEGMENT
	char	DW	3 DUP(0)
	inta	DW	5 DUP(0)
	float	DW	2 DUP(0)
	TEMP	DW	3 DUP(0)
	CONST	DW	1,100,0,2,3,100,4.58673e-41
DSEG	ENDS
CSEG	SEGMENT
	ASSUME	CS:CSEG,DS:DSEG
FUNC	PROC	NEAR
	MOV	AX,CONST[4]
	MOV	char[12],AX
	MOV	AX,char[12]
	MOV	BX,CONST[6]
	ADD	AX,BX
	MOV	char[12],AX
	MOV	AX,char[12]
	CALL	COUT
	RET
FUNC	ENDP
FUNCTT	PROC	NEAR
	CALL	FUNC
	RET
FUNCTT	ENDP
COUT	PROC	NEAR
	MOV	SI,10
	MOV	CX,0
next1:	MOV	DX,0
	DIV	SI
	PUSH	DX
	INC	CX
	CMP	AX,0
	JNZ	next1
next2:	POP	DX
	ADD	DL,30H
	MOV	AH,02H
	INT	21H
	LOOP	next2
	RET
COUT	ENDP
start:	MOV	AX,DSEG
	MOV	DS,AX
	MOV	AX,CONST[2]
	MOV	char[0],AX
	MOV	AX,CONST[8]
	MOV	char[24],AX
	MOV	AX,CONST[6]
	MOV	char[851840],AX
	MOV	AX,TEMP[2]
	AND	AX,AX
	JZ	t0
	MOV	AX,char[851840]
	CALL	COUT
t0:	MOV	AX,char[24]
	INC	AX
	MOV	char[24],AX
	MOV	AX,char[24]
	CALL	COUT
	CALL	FUNCTT
CSEG	ENDS
	END	START
